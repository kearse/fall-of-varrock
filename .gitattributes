# Normalize line endings; keep platform-specific launchers correct
* text=auto
*.bat text eol=crlf
gradlew text eol=lf

# Binary assets
*.png binary
*.jar binary
*.dat2 binary
*.idx* binary
# Shell scripts are scp'd from Windows checkouts to the VPS - CRLF breaks bash there
*.sh text eol=lf
